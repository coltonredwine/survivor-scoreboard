# Assets

Drop your optional visual assets in this folder:

- `logo.png` (or `logo.jpg`, `logo.svg` if you update `public/index.html`)
- `font.otf` for the scoreboard font

If `font.otf` is missing, the app falls back to system fonts.
If `logo.png` is missing, the logo area is hidden automatically.
